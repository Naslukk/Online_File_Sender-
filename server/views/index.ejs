<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body style="display: flex; flex-direction: column; align-items: center; justify-content: center;">
    
    <form action="/upload" method="post" enctype="multipart/form-data" style="display: grid; gap: 2rem; grid-template-columns: auto 1fr; max-width: 500px; margin: 0 auto;">
        <label for="file">File</label>
        <input type="file" id="file" name="file" required>
        <label for="password">Password</label>
        <input type="password" id="password" name="password">
        <button type="submit" style="grid-column: span 2;">Share</button>
    </form>
    <% if (locals.fileLink != null) { %>
        <div style="margin-top: 20px;">
            Your file is uploaded to
            <a href="<%= locals.fileLink %>">
                <%= locals.fileLink %>
            </a>
        </div>
    <% } %>
    
</body>
</html>